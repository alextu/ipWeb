{
    attributes = (
        {
            columnName = "CPT_LOGIN"; 
            externalType = VARCHAR2; 
            name = cptLogin; 
            valueClassName = NSString; 
            valueType = S; 
            width = 20; 
        }, 
        {
            allowsNull = Y; 
            columnName = DIPLOME; 
            externalType = VARCHAR2; 
            name = diplome; 
            valueClassName = NSString; 
            valueType = S; 
            width = 131; 
        }, 
        {
            allowsNull = N; 
            columnName = "FANN_KEY"; 
            externalType = NUMBER; 
            name = fannKey; 
            precision = 4; 
            valueClassName = NSNumber; 
            valueType = i; 
        }, 
        {
            allowsNull = N; 
            columnName = "FSPN_KEY"; 
            externalType = NUMBER; 
            name = fspnKey; 
            precision = 6; 
            valueClassName = NSNumber; 
            valueType = i; 
        }
    ); 
    attributesUsedForLocking = (cptLogin, fannKey, fspnKey); 
    className = EOGenericRecord; 
    classProperties = (cptLogin, diplome, fannKey, fspnKey); 
    externalName = "V_DIPLOME_VISIBLES"; 
    fetchSpecificationDictionary = {}; 
    internalInfo = {"_clientClassPropertyNames" = (diplome, fannKey, fspnKey); }; 
    name = VDiplomeVisibles; 
    primaryKeyAttributes = (cptLogin, fannKey, fspnKey); 
}
